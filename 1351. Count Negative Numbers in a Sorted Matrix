class Solution {
public:
    int countNegatives(vector<vector<int>>& grid) {
        int cnt=0;
        for(auto x: grid)
        {
            for(auto v: x)
            {
                if(v<0)
                cnt++;
            }
        }
        return cnt;
        
    }
};
